# Archived initial planning

These files are retained only as historical planning artifacts. They are
outdated and must not be used as implementation guidance or a source of current
project requirements.

Use:

- GitHub issues for current requirements, priorities, and implementation scope.
- Accepted records in `docs/adr/` for architectural decisions.

The root `CONTEXT.md` remains a terminology glossary, not an authority for
requirements or architecture.
