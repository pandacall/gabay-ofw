# Top-level, human-edited dependency spec. The deployed container installs
# from requirements-lock.txt instead (a full `pip freeze`, regenerated
# whenever this file changes) so a transitive upstream release can never
# change what ships to the demo (issue #49). google-adk is exact-pinned
# here too since ADK's before-tool-callback and App(...) config surface
# is a correctness dependency of ROUTING_GUARD and the topology (ADR-0004).
google-adk==2.8.0
google-genai>=2.22
fastapi>=0.115
uvicorn[standard]>=0.30
firebase-admin>=6.5
google-cloud-firestore>=2.16
google-cloud-secret-manager>=2.20
pydantic>=2.7
fpdf2>=2.8
